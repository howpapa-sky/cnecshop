# CLAUDE.md — cnecshop

> This file provides context for AI assistants working on this repository.

## Project Overview

**cnecshop** is a new project under `howpapa-sky/cnecshop`. The repository is currently in its initial setup phase with no application code committed yet.

This file serves as the foundational guide for AI assistants and will be updated as the project evolves.

## Repository Status

- **State**: Empty / initial setup
- **Default branch**: To be established with the first commit
- **Package manager**: TBD
- **Framework**: TBD

## Development Conventions

### Git Workflow

- Write clear, descriptive commit messages focused on **why**, not just what
- Use conventional commit prefixes: `feat:`, `fix:`, `docs:`, `chore:`, `refactor:`, `test:`, `style:`
- Keep commits atomic — one logical change per commit
- Do not force-push to shared branches

### Code Style

- Follow the linter and formatter configuration established in the project
- Prefer explicit over implicit
- Keep functions small and focused
- Use meaningful variable and function names

### File Organization

- Do not create files unless necessary — prefer editing existing files
- Do not create README or documentation files unless explicitly requested
- Keep the directory structure flat where possible; nest only when logical grouping is needed

## Commands

> To be populated once the tech stack is established. Expected sections:

```bash
# Install dependencies
# TBD

# Run development server
# TBD

# Run tests
# TBD

# Lint / format
# TBD

# Build for production
# TBD
```

## Architecture

> To be documented once application code is added. This section should cover:
> - Directory structure and module boundaries
> - Key architectural patterns (e.g., MVC, component-based, serverless)
> - Data flow and state management
> - Database and ORM setup
> - API structure and routing
> - Authentication and authorization approach

## Testing

> To be documented once a test framework is configured. This section should cover:
> - Test framework and runner
> - Test file naming conventions and locations
> - How to run specific test suites
> - Coverage requirements

## Environment & Configuration

> To be documented once environment setup is defined. Expected:
> - Required environment variables
> - Configuration files and their purposes
> - Local development setup instructions

## Important Notes for AI Assistants

- Always read files before modifying them
- Do not introduce security vulnerabilities (SQL injection, XSS, command injection, etc.)
- Do not over-engineer — implement only what is requested
- Do not add comments, docstrings, or type annotations to unchanged code
- Avoid creating abstractions for one-time operations
- When unsure about a convention, check existing code for patterns before asking
